fn fib(x int) int {
    if x == 0 {
        return 0;
    } else if x == 1 {
        return 1;
    }
    return fib(x - 1) + fib(x - 2);
}

fn gcd(x int, y int) int {
    if y == 0 {
        return x;
    }
    return gcd(y, x - (x / y * y));
}

fn main() int {
    return gcd(64, 128);
}
